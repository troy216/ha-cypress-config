{
  "config": {
    "step": {
      "user": {
        "title": "OIDC Provider Setup",
        "description": "Set up OIDC Provider for Home Assistant. This will enable your Home Assistant instance to act as an OpenID Connect provider for external applications."
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single instance is allowed."
    }
  }
}
